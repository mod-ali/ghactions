name: First workflow
on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcome hereee"

      - name: Checkout repo
        uses: actions/checkout@v5.0.0

      - name: list repo files 
        run: ls -la

